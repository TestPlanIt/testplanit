import { describe, it } from "vitest";

describe("Wave 3 INT-05 tiptap → markdown serializer", () => {
  it.todo(
    "serializes paragraph + heading + bullet list to GitHub-flavored markdown"
  );
  it.todo("serializes table node to pipe-separated rows");
  it.todo("preserves link href in trailing paragraph");
});
