import { describe, it } from "vitest";

describe("Wave 3 INT-05 iteration issue body builder", () => {
  it.todo("builds TipTap doc with prose lead + parameter table + deep link");
  it.todo("redacts sensitive parameter values via redactValues");
  it.todo("deep link matches matrix popover URL format (D-09)");
});
