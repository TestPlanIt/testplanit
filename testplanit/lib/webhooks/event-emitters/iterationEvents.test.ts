import { describe, it } from "vitest";

describe("Wave 2 INT-04 contract", () => {
  it.todo("emits iteration.result.recorded with redacted values");
  it.todo("requires a transaction client");
  it.todo("respects D-06 opt-in via subscribedEvents");
});
