*.zmodel

# Generated by ZenStack / OpenAPI
lib/hooks/
lib/openapi/zenstack-openapi.json
lib/openapi/openapi.json

# Handlebars templates (prettier HTML parser chokes on block helpers)
*.hbs

# Helm chart — Go-template YAML ({{ ... }}); prettier's YAML parser can't parse it
helm/

# Lockfiles
pnpm-lock.yaml

# Build output
dist/

# Auto-generated by semantic-release
CHANGELOG.md

# Local GSD planning artifacts (never committed)
.planning/

# Local runtime state (all gitignored — never committed)
.claude/
docker-data/
test-results/

# Capacity-test outputs are gitignored at `load-tests/capacity/.gitignore`,
# but prettier's --ignore-path only reads the top-level .gitignore, so the
# nested rule is invisible to format:check. Mirror it here so a local run
# of the capacity suite doesn't break precommit on subsequent PRs.
load-tests/capacity/results/
