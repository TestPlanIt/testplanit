"use client";

/**
 * Stub — Task 3 of Plan 02-04 replaces this with the real papaparse-driven
 * paste-CSV dialog. Kept as a placeholder so `DatasetTab.tsx` compiles
 * during the staged TDD execution of this plan.
 *
 * This file will be overwritten by Task 3.
 */
export interface PasteCsvDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  caseId: number;
  parameters: unknown[];
}

export function PasteCsvDialog(_props: PasteCsvDialogProps) {
  return null;
}
