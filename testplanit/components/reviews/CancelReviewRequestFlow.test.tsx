import { describe, it } from "vitest";

// Integration test composing ReviewStatusBanner + CancelRequestButton.
// Lifted from `describe.skip` once Task 2 lands ReviewStatusBanner; the full
// PENDING → cancel → null happy path is exercised there.
describe.skip("CancelReviewRequestFlow (composes Banner + CancelRequestButton)", () => {
  it("placeholder — assertions land in Task 2", () => {
    // Intentionally empty until Task 2.
  });
});
