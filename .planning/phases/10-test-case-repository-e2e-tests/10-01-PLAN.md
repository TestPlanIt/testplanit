---
phase: 10-test-case-repository-e2e-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - testplanit/e2e/tests/repository/Test Repository Management/test-case-management.spec.ts
  - testplanit/e2e/tests/repository/Test Repository Management/bulk-operations.spec.ts
autonomous: true
requirements: [REPO-01, REPO-02, REPO-03, REPO-04, REPO-05, REPO-07, REPO-08, REPO-09, REPO-10]

must_haves:
  truths:
    - "User can edit a test case name and save changes via the detail page"
    - "User can delete a test case from the detail page"
    - "User can bulk move selected test cases to a different folder"
  artifacts:
    - path: "testplanit/e2e/tests/repository/Test Repository Management/test-case-management.spec.ts"
      provides: "Test case edit and delete E2E tests"
    - path: "testplanit/e2e/tests/repository/Test Repository Management/bulk-operations.spec.ts"
      provides: "Bulk move to folder E2E test"
  key_links:
    - from: "test-case-management.spec.ts"
      to: "/en-US/projects/repository/{projectId}/{caseId}"
      via: "page.goto to case detail page, edit mode, save"
      pattern: "editButton.*click.*saveButton.*click"
    - from: "bulk-operations.spec.ts"
      to: "bulk-edit-button → bulk edit modal → move folder"
      via: "select cases, open modal, change folder, save"
      pattern: "bulk-edit-button.*folder.*Save"
---

<objective>
Fill coverage gaps for test case CRUD (REPO-01) and bulk move (REPO-03) by adding targeted tests to existing spec files.

Purpose: Existing specs cover create, view, and click-to-detail but lack explicit edit (update name + save) and delete tests for individual test cases. Bulk operations spec covers edit/delete but not bulk move to folder.

Output: Updated test-case-management.spec.ts with edit/delete tests, updated bulk-operations.spec.ts with bulk move test.

**Pre-satisfied requirements (no new tests needed):**
- REPO-02: folder-creation (11 tests), folder-edit (3), folder-delete (6) fully cover create/rename/move/delete/nested
- REPO-04: search-filter (12 tests), field-filters (8), custom-fields (14) fully cover text search, field filters, tag/state filters
- REPO-05: export-import (11 tests), markdown-export-import (4), markdown-paste-and-import (3) fully cover CSV/PDF/markdown
- REPO-07: version-history (10 tests) + steps-display diffs fully cover versions, navigation, restore
- REPO-08: tags (14 tests) fully cover create/assign/remove/edit/delete/case-insensitive
- REPO-09: issues (8 tests) adequately cover UI elements (no integration to test against)
- REPO-10: drag-drop (10 tests), tree-navigation (13 tests) fully cover reordering and navigation
</objective>

<execution_context>
@/Users/bderman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bderman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@testplanit/e2e/tests/repository/Test Repository Management/test-case-management.spec.ts
@testplanit/e2e/tests/repository/Test Repository Management/bulk-operations.spec.ts
@testplanit/e2e/fixtures/api.fixture.ts
@testplanit/e2e/page-objects/repository/repository.page.ts
@testplanit/e2e/page-objects/repository/test-case.page.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test case edit and delete E2E tests</name>
  <files>testplanit/e2e/tests/repository/Test Repository Management/test-case-management.spec.ts</files>
  <read_first>
    - testplanit/e2e/tests/repository/Test Repository Management/test-case-management.spec.ts
    - testplanit/e2e/page-objects/repository/test-case.page.ts
    - testplanit/e2e/page-objects/repository/repository.page.ts
    - testplanit/e2e/fixtures/api.fixture.ts
  </read_first>
  <action>
Add two new tests to the existing "Test Case Management" describe block in test-case-management.spec.ts:

1. **"Edit Test Case Name via Detail Page"** — Creates project+folder+case via API. Navigates to `/en-US/projects/repository/{projectId}/{caseId}`. Clicks the Edit button (`[data-testid="edit-test-case-button"]`). Finds the case name input (look for `[data-testid="case-name-input"]` or the textarea/input near the top that contains the case name). Clears and types a new name. Clicks the Save button (`button[type="submit"]:has(svg.lucide-save)` or `button:has-text("Save")`). Waits for edit button to reappear (back to view mode). Verifies the new name is visible on the page. Also verifies via `page.reload()` that the name persists.

2. **"Delete Test Case via Row Action"** — Creates project+folder+case via API. Navigates to repository page, selects folder. Finds the test case row by `[data-row-id="{caseId}"]`. Hovers to reveal action buttons. Clicks the action menu button (the row should have a kebab/more menu or direct delete button — check the row for `button:has(svg.lucide-ellipsis-vertical)` or `button:has(svg.lucide-trash-2)`). If there's a context menu, looks for "Delete" menu item. Confirms deletion in the alert dialog. Verifies the test case row is no longer visible.

Follow existing patterns: use `getTestProjectId(api)` helper, `Date.now()` for unique names, `page.waitForLoadState("networkidle")` after mutations.
  </action>
  <verify>
    <automated>cd testplanit && pnpm build && E2E_PROD=on pnpm test:e2e e2e/tests/repository/Test\ Repository\ Management/test-case-management.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - test-case-management.spec.ts contains test named "Edit Test Case Name via Detail Page"
    - test-case-management.spec.ts contains test named "Delete Test Case via Row Action"
    - Both tests use the existing getTestProjectId helper and Date.now() for unique names
    - Edit test verifies name change persists after save (checks page content after save completes)
    - Delete test verifies case row disappears after deletion
  </acceptance_criteria>
  <done>test-case-management.spec.ts has 6 tests (4 existing + 2 new), all passing against production build</done>
</task>

<task type="auto">
  <name>Task 2: Add bulk move to folder E2E test</name>
  <files>testplanit/e2e/tests/repository/Test Repository Management/bulk-operations.spec.ts</files>
  <read_first>
    - testplanit/e2e/tests/repository/Test Repository Management/bulk-operations.spec.ts
    - testplanit/e2e/page-objects/repository/repository.page.ts
    - testplanit/e2e/fixtures/api.fixture.ts
    - testplanit/components/BulkEditCases.tsx
  </read_first>
  <action>
Add one new test to the existing "Bulk Operations" describe block in bulk-operations.spec.ts:

**"Bulk Move Test Cases to Different Folder"** — Creates project, source folder, target folder, and 2 test cases in source folder via API. Navigates to repository, selects source folder. Selects both cases via row checkboxes (`[data-row-id="{id}"] button[role="checkbox"]`). Clicks bulk edit button (`[data-testid="bulk-edit-button"]`). Waits for bulk edit modal. Enables the "Folder" field by clicking its checkbox (look for `#edit-folder` or similar checkbox pattern matching the state/tags pattern already in bulk-operations.spec.ts). Selects the target folder from the folder dropdown/combobox. Clicks Save. Waits for modal to close. Navigates to target folder and verifies both cases are now visible there. Also verifies source folder is now empty (or cases are gone from it).

Read BulkEditCases.tsx component to find the exact folder field checkbox ID and folder selector pattern. The component likely uses a folder combobox or select similar to the state selector already tested.
  </action>
  <verify>
    <automated>cd testplanit && pnpm build && E2E_PROD=on pnpm test:e2e e2e/tests/repository/Test\ Repository\ Management/bulk-operations.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - bulk-operations.spec.ts contains test named "Bulk Move Test Cases to Different Folder"
    - Test creates source and target folders with test cases
    - Test selects cases, opens bulk edit modal, changes folder, saves
    - Test verifies cases appear in target folder after move
    - Test verifies cases no longer appear in source folder
  </acceptance_criteria>
  <done>bulk-operations.spec.ts has 7 tests (6 existing + 1 new), all passing against production build</done>
</task>

</tasks>

<verification>
Run the full repository E2E test suite to confirm no regressions:
```bash
cd testplanit && pnpm build && E2E_PROD=on pnpm test:e2e e2e/tests/repository/
```
</verification>

<success_criteria>
- test-case-management.spec.ts has edit and delete tests passing
- bulk-operations.spec.ts has bulk move test passing
- All existing repository tests still pass (no regressions)
- REPO-01 gap (edit/delete) is closed
- REPO-03 gap (bulk move) is closed
</success_criteria>

<output>
After completion, create `.planning/phases/10-test-case-repository-e2e-tests/10-01-SUMMARY.md`
</output>
