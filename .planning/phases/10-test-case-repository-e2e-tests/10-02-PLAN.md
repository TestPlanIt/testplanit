---
phase: 10-test-case-repository-e2e-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - testplanit/e2e/tests/repository/Test Repository Management/shared-steps.spec.ts
autonomous: true
requirements: [REPO-06]

must_haves:
  truths:
    - "User can create a shared step group from the shared steps page"
    - "User can edit a shared step group name and its step items"
    - "User can use a shared step group in a test case"
    - "Shared step group changes create new versions visible in version history"
  artifacts:
    - path: "testplanit/e2e/tests/repository/Test Repository Management/shared-steps.spec.ts"
      provides: "Shared steps CRUD and versioning E2E tests"
  key_links:
    - from: "shared-steps.spec.ts"
      to: "/en-US/projects/shared-steps/{projectId}"
      via: "page.goto to shared steps page"
      pattern: "goto.*shared-steps"
    - from: "shared-steps.spec.ts"
      to: "/en-US/projects/repository/{projectId}/{caseId}"
      via: "Navigate to case detail to verify shared step usage"
      pattern: "goto.*repository"
---

<objective>
Create E2E tests for shared step group CRUD and versioning (REPO-06).

Purpose: The existing steps-display.spec.ts only tests rendering of shared steps within test case detail views. It does not test the dedicated shared steps management page where users create, edit, and manage shared step groups. This is the primary gap for REPO-06.

Output: New shared-steps.spec.ts covering create, edit, delete, use in test case, and version history of shared step groups.

**Existing coverage:** steps-display.spec.ts has "Shared Step Group Display" test that creates shared steps via API and verifies they render on a test case detail page. This is display-only — no UI-driven CRUD.
</objective>

<execution_context>
@/Users/bderman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bderman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@testplanit/e2e/tests/repository/Test Repository Management/steps-display.spec.ts
@testplanit/e2e/fixtures/api.fixture.ts
@testplanit/e2e/page-objects/repository/repository.page.ts

<interfaces>
<!-- The shared steps page is at app/[locale]/projects/shared-steps/[projectId]/page.tsx -->
<!-- It likely renders a list/table of shared step groups with CRUD actions -->
<!-- ManualSharedStepsDialog.tsx handles creating/editing shared step groups -->
<!-- The API model name is sharedStepGroup (singular camelCase per CLAUDE.md conventions) -->

From api.fixture.ts:
```typescript
async createSharedStepGroup(projectId: number, name: string, items: Array<{
  step: Record<string, unknown>;
  expectedResult: Record<string, unknown>;
  order: number;
}>): Promise<number>;

async addSharedStepGroupToTestCase(testCaseId: number, sharedStepGroupId: number, order: number): Promise<number>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared steps CRUD E2E tests</name>
  <files>testplanit/e2e/tests/repository/Test Repository Management/shared-steps.spec.ts</files>
  <read_first>
    - testplanit/e2e/tests/repository/Test Repository Management/steps-display.spec.ts
    - testplanit/e2e/fixtures/api.fixture.ts
    - testplanit/app/[locale]/projects/shared-steps/[projectId]/page.tsx
    - testplanit/app/[locale]/projects/shared-steps/[projectId]/ManualSharedStepsDialog.tsx
    - testplanit/e2e/page-objects/repository/repository.page.ts
    - testplanit/e2e/page-objects/repository/test-case.page.ts
  </read_first>
  <action>
Create a new file `testplanit/e2e/tests/repository/Test Repository Management/shared-steps.spec.ts` with these tests:

**Test 1: "Create Shared Step Group via UI"**
- Create project via API
- Navigate to shared steps page: `/en-US/projects/shared-steps/{projectId}`
- Read the page.tsx to understand the UI (button to open ManualSharedStepsDialog, table of groups)
- Click the "Add" or "Create" button to open the shared steps dialog
- Read ManualSharedStepsDialog.tsx to understand the form structure
- Fill in the group name
- Add at least 2 step items (step text + expected result for each)
- Submit/save the dialog
- Verify the new shared step group appears in the list/table

**Test 2: "Edit Shared Step Group Name and Steps"**
- Create project + shared step group via API (using createSharedStepGroup)
- Navigate to shared steps page
- Find the shared step group in the list
- Click edit (look for edit button/link in the row)
- Change the group name
- Edit the step text of the first item
- Add a new step item
- Save changes
- Verify updated name and step count are reflected in the list

**Test 3: "Delete Shared Step Group"**
- Create project + shared step group via API
- Navigate to shared steps page
- Find the group in the list
- Click delete button
- Confirm in dialog
- Verify group is removed from the list

**Test 4: "Use Shared Step Group in Test Case"**
- Create project + folder + test case + shared step group via API
- Navigate to test case detail page
- Enter edit mode
- Look for the "Add Shared Step" button (read the case detail page to find exact UI)
- Add the shared step group reference
- Save
- Verify the shared step group is displayed in view mode (with Layers icon and group name visible, like the existing steps-display.spec.ts test verifies)

**Test 5: "Shared Step Group Version History"**
- Create project + shared step group via API
- Edit the shared step group via API (update name or steps) to create a version
- Navigate to shared steps page
- Verify version indicator is visible (if the UI shows versions)
- OR navigate to a test case using this shared step group and verify the version of the shared step group is displayed

Follow existing test patterns:
- Import from `"../../../fixtures"` (test, expect)
- Use `api.createProject(...)` for isolation
- Use `Date.now()` for unique names
- Use `page.waitForLoadState("networkidle")` after navigation/mutations
- Use `page.locator('[role="dialog"]')` for modals

IMPORTANT: Read the actual page.tsx and ManualSharedStepsDialog.tsx before writing tests to understand the real UI structure (test IDs, button text, form fields, table columns). Do NOT guess at selectors.
  </action>
  <verify>
    <automated>cd testplanit && pnpm build && E2E_PROD=on pnpm test:e2e e2e/tests/repository/Test\ Repository\ Management/shared-steps.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists: testplanit/e2e/tests/repository/Test Repository Management/shared-steps.spec.ts
    - Contains at least 4 test cases (create, edit, delete, use in test case)
    - Create test navigates to shared steps page and creates a group via UI
    - Edit test modifies group name and/or steps via UI
    - Delete test removes a group and verifies it's gone
    - Use test adds shared step group to a test case and verifies it renders
    - All tests use unique project/data isolation via API helpers
  </acceptance_criteria>
  <done>shared-steps.spec.ts exists with 4-5 tests covering create, edit, delete, and use in test case, all passing against production build</done>
</task>

</tasks>

<verification>
Run shared steps tests and existing steps display tests to confirm no conflicts:
```bash
cd testplanit && pnpm build && E2E_PROD=on pnpm test:e2e e2e/tests/repository/Test\ Repository\ Management/shared-steps.spec.ts e2e/tests/repository/Test\ Repository\ Management/steps-display.spec.ts
```
</verification>

<success_criteria>
- shared-steps.spec.ts tests pass against production build
- Shared step CRUD is verified end-to-end via UI
- Shared step usage in test cases is verified
- REPO-06 gap is closed
</success_criteria>

<output>
After completion, create `.planning/phases/10-test-case-repository-e2e-tests/10-02-SUMMARY.md`
</output>
